# 字节流重组

按 32 位回绕序号把分片拼回字节流。`Ingest(seq, payload)` 送入，`Emit()` 取出连续前缀。

- 序号按无符号 32 位回绕比较（以最近半个序号空间为准）。
- 重叠位置以先送入的字节为准，后到的重复字节丢弃。
- 中间有空洞时只吐出从头开始的连续前缀，空洞之后的内容继续缓存，补齐后再吐。
- `New(limit)` 限制缓存的字节总数；顶满时丢弃最老空洞之后序号最小的乱序缓存。

拉起环境：

```
docker compose up --build --abort-on-container-exit
```
